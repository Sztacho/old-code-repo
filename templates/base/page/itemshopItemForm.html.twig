{% extends 'base/base.html.twig' %}

{% block body %}
    {% for form in formList %}
        {% if form is not null %}
            {{ form_start(form) }}
            {{ form_end(form) }}
        {% endif %}
    {% endfor %}
{% endblock %}
